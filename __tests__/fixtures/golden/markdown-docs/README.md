# Widget

A widget with a documented pipeline. See [the architecture](docs/architecture.md) and `src/widget.ts`.

## Installation

Install the widget.

## Usage

Call `createWidget` from `src/widget.ts`, then `render` it.
