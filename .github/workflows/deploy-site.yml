name: Deploy site to GitHub Pages

# Pages publishing is disabled on this fork.
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages publishing is disabled on this fork."
